package example_app;

// Application specific stuff.  TODO: Move into "package
// protobuf_encoder_demo".
enum FieldId {
  NAME_FIELD = 0;
  ADDRESS_FIELD = 1;
}

// TODO: Put Schema IDs here?  Get rid of field IDs; those can be in the
// application.  We just need their names.
